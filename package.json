{
  "name": "proposal-dapp2",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "client:dev": "cd client && yarn dev",
    "client:install": "cd client && yarn",
    "contract:compile": "cd contract && npx hardhat compile",
    "contract:deploy": "cd contract && npx hardhat run --network localhost scripts/deploy.ts",
    "contract:install": "cd contract && yarn",
    "contract:launch-network": "cd contract && npx hardhat node",
    "project:install": "yarn client:install && yarn contract:install && yarn server:install",
    "dev": "yarn server:dev && yarn client:dev",
    "server:dev": "cd server && yarn start:dev",
    "server:install": "cd server && yarn"
  }
}
